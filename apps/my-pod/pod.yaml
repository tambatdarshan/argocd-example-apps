apiVersion: v1
kind: Pod
metadata:
  name: gitops-standalone-pod
  namespace: default
  labels:
    app: gitops-demo
spec:
  containers:
  - name: web-server
    image: nginx:alpine
    ports:
    - containerPort: 80
